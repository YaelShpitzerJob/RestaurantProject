// import { selectCreateContactDomain } from '../selectors';

describe('selectCreateContactDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
